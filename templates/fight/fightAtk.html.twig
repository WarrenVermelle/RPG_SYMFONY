{% extends "base.html.twig" %}

{% block body %}
    <div>
        <p>{{champion.name}}</p>
        <p>Level: {{champion.level}}</p>
        <p>Hp: {{champion.hp}}</p>
    </div>

    <div>
        <p>{{monster.monster}}</p>
        <p>Level: {{monster.level}}</p>
        <p>Hp: {{monster.hp}}</p>
    </div>
{% endblock %}