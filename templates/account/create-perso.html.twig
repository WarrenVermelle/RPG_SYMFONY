{% extends "base.html.twig" %}

{% block title %}Création de personnage{% endblock %}

{% block body %}
<div id="affcrepers">
    <h1 id="titremoncompte">Créer votre personnage</h1>

    <div id="formcreaperso">
        {{ form_start(formPerso ) }}
                {{ form_row(formPerso.name , {
                    label: 'Name : '
                }) }}
                {{ form_row(formPerso.gender , {
                    label: 'Genre : '
                }) }}
                {{ form_row(formPerso.race, {
                    label: 'Race : '
                }) }}
                {{ form_row(formPerso.faction
                , {
                    label: 'Faction : ' 
                }) }}
                {% if champion.gender is not null and champion.race is not null and champion.faction is null %}
                    <img src="{{ vich_uploader_asset(champion.race, champion.currentImage) }}" alt="">
                {% endif %}
                {% if champion.gender is not null and champion.race is not null and champion.faction is not null %}
                    <img src="{{ vich_uploader_asset(champion.faction, champion.currentImage) }}" alt="">
                {% endif %}

                <button type="submit" class="btn" id="subcreapers">Créez !</button>
        {{ form_end(formPerso) }}
    </div>
</div>

{% endblock %}