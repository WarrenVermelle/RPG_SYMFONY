<form method="post" action="{{ path('race_delete', {'id': race.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette race ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race.id) }}">
    <button class="btn">Delete</button>
</form>
