{% extends "base.html.twig" %}

{% block title %}Edition de l'utilisateur{% endblock %}

{% block body %}
<div class="listeform">
    <h1>Editer l'utilisateur</h1>

    <div>
        {{ form_start(form, {'attr': {'class': 'form'}}) }}
                {{ form_widget(form, {'attr': {'class': 'form2'}}) }}
                <button type="submit" class="btn" id="">Editer</button>
        {{ form_end(form) }}
    </div>
    <div class="retourform"><a href="{{ path("admin_userslist") }}">Retour</a></div>
</div>

{% endblock %}